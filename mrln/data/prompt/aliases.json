{
  "_comment": "Old slug -> new slug, consulted only when a lookup misses. Empty pre-release: nothing has shipped, so pre-release renames were remapped directly instead of aliased. From the first release on, every renamed factory slug MUST get an entry here (lint: test_factory_aliases_valid). Chains are allowed; targets may be leaf sections or folders.",
  "sections": {},
  "templates": {}
}
